# Local tool deployment for pipeline support

this configure and deploy jenkins and vault using docker compose

# prerequis

this runs in Linux envirotments and need:

- docker installed.
- this need commonLibs.sh.